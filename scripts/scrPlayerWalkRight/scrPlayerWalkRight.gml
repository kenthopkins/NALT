//Player Walk Right

sprite_index = sPlayerR;
if (key_right) 
{
var move = key_right - key_left;
hsp = move * walksp; 
vsp = vsp + grv
if (place_meeting(x,y+1,oWall01)) && (key_jump)
	{
	vsp = -8;
	}
}